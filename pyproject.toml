[project]
name = "persian-writing-app"
version = "0.1.0"
description = "Persian book writing assistant CLI"
authors = [{name = "Auto-generated", email = "example@example.com"}]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
persian-writer = "persian_writer.cli:main"
